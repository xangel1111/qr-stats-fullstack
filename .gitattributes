# Normalize line endings to LF in the repository (consistent across OSes and
# safe for Dockerfiles, shell and config files).
* text=auto eol=lf

# Binary assets
*.png binary
*.jpg binary
*.ico binary
